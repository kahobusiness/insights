.DS_Store
node_modules
.next
_pagefind/
tsconfig.tsbuildinfo

# Logs
*.log

# Environment files
.env*.local

# Vercel
.vercel

# Editor
.vscode
# Regenerated by every `next dev` / `next build` with differing paths (.next/dev/types vs .next/types); ignored as in create-next-app's template
next-env.d.ts
